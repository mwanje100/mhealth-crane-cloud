version: '3.8'

services:
  edge-node:
    build: .
    ports:
      - "3030:3030"
    networks:
      mhealth-net:
        ipv4_address: 172.20.0.2

networks:
  mhealth-net:
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/16
